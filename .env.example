# Copy to .env and fill in. The reports pipeline reads these from the environment.
# (.env is gitignored. Load it however you prefer — e.g. `set -a; . ./.env; set +a`.)

# --- LLM (bring your own — fully provider-agnostic, via LiteLLM) ---
# The synthesis layer talks only to common/llm.py; the model + provider come entirely from here.
LLM_MODEL=                     # LiteLLM model string, e.g. gpt-4o  |  claude-3-5-sonnet-latest  |  <vendor>/<model>
LLM_API_KEY=                   # your provider API key (omit if the backend uses ambient credentials)
# LLM_BASE_URL=                # optional: a custom API base URL (self-hosted, gateway, router…)
# LLM_SEARCH_TOOL=             # optional JSON: the web-search tool spec your model accepts, to enable the
#                              # grounded discovery features (screeners). Leave unset to run un-grounded.
#
# The model string selects the provider (see LiteLLM's provider list). Some backends read their own
# extra env vars (e.g. a project/region/credentials file for a cloud provider, or an org id) — add
# whatever your chosen model needs here; it all stays in this gitignored file.
#
# Capabilities that degrade gracefully: PDF filing-reading (deep report) and web-grounded discovery
# (screeners) run only where the model supports them; elsewhere the call is text-only / un-grounded.

# --- Email bot (interactive reports + watchlist digest) ---
# The bot's account is ONE Gmail used to both send reports and read requests.
# Gmail: enable 2-Step Verification, then create an App Password (Account ->
# Security -> App passwords) and use it for SMTP_PASS and IMAP_PASS.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@gmail.com
SMTP_PASS=your-16-char-app-password
IMAP_HOST=imap.gmail.com
IMAP_PORT=993
IMAP_USER=you@gmail.com
IMAP_PASS=your-16-char-app-password
REPORT_FROM=you@gmail.com
# Daily digest is sent here; and ONLY these senders may request reports
# (comma-separated). You email the bot FROM one of these (e.g. a 2nd address).
REPORT_TO=you@gmail.com
EMAIL_ALLOWED_SENDERS=you@work.com

# --- Watchlist seed (scripts/populate_watchlist.py) ---
# Your personal portfolio is NOT hardcoded in the repo — it's seeded from here.
# Comma-separated company names OR NSE symbols. Leave blank to skip.
WATCHLIST_HOLDINGS=reliance industries, hdfc bank, tata consultancy services
WATCHLIST_TRACKING=infosys, larsen and toubro

# --- NSE browser-tier opt-in ---
# The NSE /api tier loads a real page to clear NSE's bot challenge. NSE's Terms
# of Use restrict automated access and redistribution of its data, so this tier
# is OFF by default. Set to true ONLY if you have reviewed NSE's terms and accept
# responsibility for complying (no redistribution of NSE data). See SOURCES.md.
NSE_SCRAPING_ENABLED=false

# Employer-reviews (AmbitionBox) browser tier for the deep report's "🏢 inside view"
# (employee & management sentiment). OFF by default — automated access is subject to
# AmbitionBox's Terms of Service; set true only for personal research. See SOURCES.md.
EMPLOYER_REVIEWS_ENABLED=false
# Optional slug overrides for companies whose name the resolver mis-maps to a namesake
# (comma-separated SYMBOL=ambitionbox-slug pairs). Kept here (not in code) so no specific
# tickers live in the repo. Example:
# EMPLOYER_SLUG_OVERRIDES=EXAMPLECO=example-co, OTHERCO=other-co-ltd

# --- Compliance ---
# Footer stamped on every emailed report + PDF. A built-in default is used when
# unset; customise it here, or set it EMPTY to disable (not recommended for a
# public-facing tool — it states the output is research, not investment advice).
# REPORT_DISCLAIMER=

# ============================================================================
# Scheduling, feature toggles & tuning  (all read by src/equity_research/config.py)
# ----------------------------------------------------------------------------
# EVERY value below is optional — each has a default equal to the project's
# original behaviour, so the bot runs fine with none of this set. Uncomment and
# change only what you want. This is an India (NSE/BSE) build: TIMEZONE mainly
# shifts *delivery* times; benchmarks / ₹-crore scale / data sources are structural.
# ============================================================================

# --- Timezone (all schedule times below are in this zone) ---
# TIMEZONE=Asia/Kolkata

# --- Delivery schedule (24h HH:MM, or an hour where noted) ---
# PUSH_PREMARKET=08:30           # pre-open GIFT-Nifty digest
# PUSH_MIDDAY=12:30              # midday same-day digest
# PUSH_EOD_HOUR=18               # full daily digest + ALL weekly pushes fire at/after this hour
# WEEKLY_PUSH_DAY=Sat            # weekday the weekly pushes run (Mon/Tue/…/Sun, or 0–6 with Mon=0)
# TAILWIND_URGENT_SLOTS=08:30,12:30,18:00   # urgent supply-shock break-in slots; EMPTY = disable urgent alerts
# HEARTBEAT_SECONDS=300          # IMAP IDLE wait + scheduler tick (keep < Gmail's ~29-min cap)
# MENU_TTL_HOURS=24              # how long a numbered "reply with N" menu stays answerable

# --- Feature toggles (true/false; all default true). Turn a scheduled push off here. ---
# (On-demand email commands keep working even when a push is disabled.)
# ENABLE_PREMARKET=true
# ENABLE_MIDDAY=true
# ENABLE_EOD_DIGEST=true
# ENABLE_SCREEN_DIGEST=true
# ENABLE_SECTOR_ROTATION=true
# ENABLE_TAILWIND=true           # gates BOTH the weekly Tailwind push and the urgent break-ins
# ENABLE_PICKAXE=true
# ENABLE_CONCALLS=true
# ENABLE_RESULTS_RADAR=true
# ENABLE_KEYWORD_ALERTS=true
# ENABLE_MAIL_HOUSEKEEPING=true  # bin processed workbench mail on the bot's own Gmail

# --- Cache / dedup lifetimes ---
# TAILWIND_CACHE_TTL_H=24        # on-demand Tailwind result cache (hours)
# HOTLIST_CACHE_TTL_H=24
# PICKAXE_CACHE_TTL_H=24
# TAILWIND_SEEN_TTL_DAYS=14      # how long an alerted shock stays de-duplicated
# EMPLOYER_CACHE_TTL_DAYS=30

# --- Timeouts (seconds) on best-effort renders / screens ---
# PDF_RENDER_TIMEOUT_S=150
# SCREEN_TIMEOUT_S=300           # generic universe-loop screens
# TAILWIND_TIMEOUT_S=420
# HOTLIST_TIMEOUT_S=420

# --- Market-cap bands (₹ crore) ---
# SMALLCAP_MAX_CR=5000
# MIDCAP_MAX_CR=25000
# LARGECAP_MAX_CR=75000
# TAILWIND_SMALLMID_CEIL_CR=25000  # a fresh shock with a beneficiary under this can trigger an urgent alert

# --- Headline tuning ---
# RISK_FREE_RATE=0.07            # DCF / CAPM / fund-Sharpe risk-free rate
# BIG_MOVE_PCT=0.06              # |1-day return| that fires a "big move" watchlist alert

# ----------------------------------------------------------------------------
# ADVANCED (rarely changed) — deep analytical thresholds. Defaults are sensible;
# override only if you know what you're tuning. All optional.
# ----------------------------------------------------------------------------
# Background-pass cadence & the keyword-alert active window
# ALERT_ACTIVE_START_HOUR=8
# ALERT_ACTIVE_END_HOUR=23       # exclusive upper bound; alerts never ping outside [start, end)
# ALERT_SCAN_GAP_MIN=20
# CONCALL_INGEST_GAP_MIN=60
# RESULTS_INGEST_GAP_MIN=60
# Result caps / batch bounds
# SCREEN_RESULT_LIMIT=20         # default rows per screen command
# TECHNICAL_LIMIT=15
# HOTLIST_LIMIT=25
# HOLDCO_LIMIT=30
# CALL_RADAR_MAX_NEW=8
# RESULTS_MAX_NEW=6
# TAILWIND_MAX_CATALYSTS=8
# TAILWIND_URGENT_MAX_CATALYSTS=2
# TAILWIND_URGENT_MAP_CAP=6
# PICKAXE_MAX_THEMES=6
# PICKAXE_DAYS=21
# TRENDS_PER_CAT=10
# TRENDS_MAX_SIGNALS=40
# NEWS_MAX_SIGNALS=70
# POLICY_MAX_CLASSIFY=55
# POLICY_LIMIT_RELEASES=120
# HOTLIST_ENGINE_DEPTH=30
# ACCUM_ENRICH_CAP=40
# SHORTLIST_CAP=60
# Screen eligibility bands (₹ crore) & signal thresholds
# SMALLCAP_BAND_LO_CR=1000
# SMALLCAP_BAND_HI_CR=10000
# HOLDCO_MIN_NAV_CR=500
# MOMENTUM_MIN_TURNOVER_CR=2.0
# MOMENTUM_MIN_DAYS=150
# MOMENTUM_NEAR_HIGH=-0.04
# MOMENTUM_MIN_VOL_SURGE=1.3
# MOMENTUM_LIQ_WINDOW=20
# LEADERS_MIN_TURNOVER_CR=10.0
# LEADERS_LIQ_WINDOW=20
# ACCUM_MIN_PROMOTER_DELTA=0.10
# OWNERSHIP_APPEAR_FLOOR=0.20
# OWNERSHIP_DELTA_FLOOR=0.10
# CALL_RADAR_LOOKBACK_DAYS=45
# CALL_RADAR_WINDOW_DAYS=35
# CALL_RADAR_GAP_THRESHOLD=2
# RESULTS_LOOKBACK_DAYS=45
# RESULTS_FRESH_DAYS=35
# RESULTS_STALE_DAYS=100
# KEYWORD_SWEEP_DAYS=3
# Bank health checks (deep report §9 for banks; all in %)
# BANK_NNPA_WARN=2.0
# BANK_NNPA_ALARM=4.0
# BANK_PCR_WARN=60.0
# BANK_CET1_WARN=10.0
# BANK_CD_RATIO_WARN=90.0
# BANK_ROA_WEAK=0.5
# BANK_ROA_STRONG=1.5
# BANK_CREDIT_COST_SPIKE_X=1.5
# BANK_NII_LAG_PP=5.0
# Projection / fund knobs & benchmarks
# PROJ_YEARS=10
# FUNDS_TRADING_DAYS=252
# FUNDS_ASOF_TOL_DAYS=12
# FUNDS_SIP_MONTHLY=10000
# MARKET_INDEX=Nifty 50
# LEADERS_BENCHMARK=Nifty 500
